# 遗留系统提示（已模块化）

默认运行时请使用 **Core + Module + Knobs** 组装结果，见：

- `ARCHITECTURE.md` — 架构说明
- `manifest.yaml` — 场景与片段顺序
- `composer.py` — `DEFAULT_COMPOSED_SYSTEM_PROMPT`

本文件仅作人工对照或 `load_legacy_system_prompt()` 应急回退，勿与 `core/`、`modules/` 双份维护。
